{
  "name": "@kendraw/renderer-svg",
  "version": "0.0.0",
  "private": true,
  "description": "SVG export renderer for publication-quality output",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  }
}
