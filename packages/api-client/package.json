{
  "name": "@kendraw/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "REST API client for Kendraw backend (stub in static-demo mode)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  }
}
